import React from 'react';
import ExpenseList from './ExpenseList'

const Dashboard = () => {
  return (
    <div>
      dashboard page
      <ExpenseList />
    </div>
  );
};

export default Dashboard;